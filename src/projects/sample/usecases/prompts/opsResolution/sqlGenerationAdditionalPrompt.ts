export const sqlGenerationAdditionalPrompt = [
  "【重大な注意点】",
  "- shop_idはテーブルによって参照先が異なるので特に注意すること",
  "  - **shopify_bulk_import_orders_workflows.shop_id = shops.ec_platform_shop_id となる**",
  "  - **job_statuses.shop_id = shops.external_shop_id となる**",
  "  - **job_statuses テーブルにはjob_classカラムは存在しない。Datadogログのcontext.job.job_classに対応するカラムはtypeである**",
  "  - **shopify_bulk_operation_statuses.shop_id = shops.id（内部ID）となる。external_shop_idではない。サブクエリで変換: WHERE shop_id = (SELECT id FROM shops WHERE external_shop_id = ...)）**",
  "  - **amazon_fba_inventories.seller_id = shops.ec_platform_shop_id となる**",
  "  - **amazon_report_listings_items.seller_id = shops.ec_platform_shop_id となる**",
  "  - それ以外は基本的にはexternal_shop_id の場合は下記のサブクエリで変換する",
  "    - 例: WHERE shop_id IN (SELECT external_shop_id FROM shops WHERE company_code = 'XX001')",
  "  - サブクエリは複数行を返す可能性があるため、= ではなく IN を使うこと（= だと「Subquery returns more than 1 row」エラーになる）",
  "  - 他のテーブルの shop_id が同じとは限らない。必ず各テーブルのEloquentモデルPHPDocやスキーマコメントで確認すること",
  "- shop_itemsテーブルとのJOIN:",
  "  - **shop_itemsテーブルのshop_idカラムは削除済み。ショップとの結合は shop_items.external_shop_id = shops.external_shop_id を使うこと**",
  "  - **stock_sync_shop_item_statuses.shop_item_id = shop_items.id となる（shop_items.shop_item_idカラムは存在しない。PKはid）**",
  "  - **stock_sync_shop_item_failure_notifications.shop_item_id = shop_items.id となる（同上）**",
  "- amazon_fba_inventories / amazon_report_listings_items のSKUカラムの区別：",
  "  - **fn_sku**: FBA専用SKU。「X000」で始まるID（例: X000WDYUTH）",
  "  - **seller_sku**: 出品者が自分で設定するSKU。fn_skuとは異なる値",
  "  - ログやデータに「X000」で始まる値が含まれている場合、それはfn_skuであり seller_sku ではない",
  "",
  "【ステータス値の確認方法（enum tracing）】",
  "- SQLでステータスカラムの値を使う場合、必ずEloquentモデルからenumクラスを辿って正確な値を確認すること",
  "- 推測やログの値をそのまま使うことは禁止（例: 'completed'や'exported'と推測してはならない。実際は'succeeded'かもしれない）",
  "- 辿り方:",
  "  1. Eloquentモデルファイルの先頭のuse文から、App\\Core\\Domain\\Entities\\...のenumクラスのimportを探す",
  "  2. toEntity()メソッド内で EnumClass::from($this->カラム名) の記述を確認し、どのカラムにどのenumが対応するか特定する",
  "     - toEntity()内に直接ない場合、statusAsEnum()等のアクセサメソッド経由の場合もある",
  "  3. enumクラスのファイルパスはnamespaceから特定する（App\\ → app/、\\ → /、末尾に.php）",
  "  4. local_readFileでenumファイルを読み、case文の値（= 'xxx'の部分）を確認する",
  "  5. confirmedFactsに「enum確認: {テーブル名}.{カラム名} → {enumクラス名}: {値の一覧}」を記載する",
  "- 全てのenumはPHP 8.1のbacked string enum（enum Foo: string）であり、case値がそのままDB格納値である",
].join("\n");
